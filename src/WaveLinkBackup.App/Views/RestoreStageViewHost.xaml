<UserControl x:Class="WaveLinkBackup.App.Views.RestoreStageViewHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:WaveLinkBackup.App.Views">

    <!--
        One stage of the restore in-progress strip (04-in-progress.md). Three treatments, driven
        by Stage.Status and applied in code-behind (OnStageChanged):

          done     14px check in WlOk + mono 500 11px ls .14em WlMuted
          current  mono 500 11px ls .14em WlStrong, 4px bottom pad, 2px solid WlAccent bottom rule
          pending  mono 500 11px ls .14em WlMuted at 40% opacity

        The model owns the status; this control only maps each one to its visual. A separate user
        control (rather than four inline DataTrigger blocks in MainWindow.xaml) keeps the treatment
        logic in one place - the strip's XAML just places four of these with their connectors.
    -->
    <Border x:Name="Rule" BorderThickness="0,0,0,2" Padding="0,0,0,4">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">

            <!-- The 14px check: visible only when done, WlOk. -->
            <Path x:Name="CheckMark" Width="14" Height="14" Stretch="Uniform"
                  Stroke="{DynamicResource WlOk}" StrokeThickness="2" Margin="0,0,6,0"
                  VerticalAlignment="Center">
                <Path.Data>
                    <PathGeometry Figures="M 1,8 L 5,12 L 13,2" />
                </Path.Data>
            </Path>

            <!-- The stage label: mono 500 11px ls .14em; colour/weight set per status below. -->
            <views:TrackedText x:Name="Label" Tracking=".14" FontSize="11"
                               VerticalAlignment="Center" />
        </StackPanel>
    </Border>

</UserControl>
