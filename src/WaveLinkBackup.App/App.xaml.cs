namespace WaveLinkBackup.App;

/// <summary>
/// Phase 5 builds the four designed screens. This stub exists from the first commit so the
/// reference graph is proven early (ADR-004) and so Core's headless guard has something real
/// to guard against.
/// </summary>
public partial class App : System.Windows.Application;
